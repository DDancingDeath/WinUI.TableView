<ResourceDictionary
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="using:WinUI.TableView"
xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:not_win="http://uno.ui/not_win"
mc:Ignorable="not_win">

<ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="ms-appx:///WinUI.TableView/Themes/Resources.xaml" />
</ResourceDictionary.MergedDictionaries>

<!-- Override OptionsButtonIcon to use ChevronDown -->
<x:String x:Key="OptionsButtonIcon">&#xE70D;</x:String>

<!-- File Explorer TableViewColumnHeader Style with Larger Filter Dropdown -->
    <Style x:Key="FileExplorerTableViewColumnHeaderStyle"
           TargetType="local:TableViewColumnHeader">
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="BackgroundSizing"
                Value="InnerBorderEdge" />
        <Setter Property="Foreground"
                Value="{ThemeResource FileExplorerHeaderTextBrush}" />
        <Setter Property="Padding"
                Value="12,0" />
        <Setter Property="UseSystemFocusVisuals"
                Value="{StaticResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin"
                Value="-3" />
        <Setter Property="CornerRadius"
                Value="0" />
        <Setter Property="VerticalAlignment"
                Value="Stretch" />
        <Setter Property="HorizontalAlignment"
                Value="Stretch" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="HorizontalContentAlignment"
                Value="Stretch" />
        <Setter Property="IsTabStop"
                Value="False" />
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Opacity="0" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TableViewColumnHeader">
                    <Grid x:Name="rootGrid">
                        <Grid.Background>
                            <SolidColorBrush Color="Transparent" />
                        </Grid.Background>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />

                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="rootGrid.Background"
                                                Value="{ThemeResource TableViewHeaderBackgroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="rootGrid.Background"
                                                Value="{ThemeResource TableViewHeaderBackgroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Margin="{TemplateBinding Padding}"
                                              Foreground="{TemplateBinding Foreground}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />

                            <Grid HorizontalAlignment="Right">
                                <FontIcon x:Name="SortIcon"
                                          FontSize="10"
                                          Margin="0,2,0,0"
                                          Visibility="Collapsed"
                                          VerticalAlignment="Top"
                                          HorizontalAlignment="Center"
                                          Glyph="{ThemeResource SortIconAscending}" />

                                <Button x:Name="OptionsButton"
                                        Padding="4"
                                        IsTabStop="False"
                                        BorderThickness="0"
                                        Visibility="Collapsed"
                                        Background="Transparent"
                                        VerticalAlignment="Bottom">

                                    <Grid>
                                        <FontIcon x:Name="OptionsIcon"
                                                  FontSize="11"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Right"
                                                  Glyph="{ThemeResource OptionsButtonIcon}" />

                                        <FontIcon x:Name="FilterIcon"
                                                  FontSize="11"
                                                  Visibility="Collapsed"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Right"
                                                  Glyph="{ThemeResource FilterIcon}" />
                                    </Grid>

                                    <Button.Flyout>
                                        <MenuFlyout x:Name="OptionsFlyout"
                                                    Placement="Bottom">
                                            <MenuFlyoutItem x:Name="ItemsCheckFlyoutItem">
                                                <MenuFlyoutItem.Template>
                                                    <ControlTemplate TargetType="MenuFlyoutItem">
                                                        <!-- Width set to 240 for compact File Explorer style, Height is Auto with MaxHeight -->
                                                        <Grid Margin="6,4"
                                                              Width="240"
                                                              MaxHeight="400">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="*" />
                                                            </Grid.RowDefinitions>
                                                             <Border Grid.Row="0"
                                                                    BorderThickness="0,0,0,1"
                                                                    BorderBrush="{ThemeResource MenuFlyoutSeparatorBackground}">
                                                                <CheckBox x:Name="SelectAllCheckBox"
                                                                          Margin="8,6,8,6"
                                                                          FontSize="12" />
                                                            </Border>
                                                            <ListView x:Name="FilterItemsList"
                                                                      Grid.Row="1"
                                                                      SelectionMode="None"
                                                                      not_win:Margin="12,0,0,0"
                                                                      ItemsSource="{Binding FilterItems}">
                                                                <ListView.ItemContainerStyle>
                                                                    <Style TargetType="ListViewItem"
                                                                           BasedOn="{StaticResource DefaultListViewItemStyle}">
                                                                        <Setter Property="Padding"
                                                                                Value="8,0" />
                                                                        <Setter Property="Margin"
                                                                                Value="0" />
                                                                        <Setter Property="MinHeight"
                                                                                Value="32" />
                                                                        <Setter Property="Height"
                                                                                Value="32" />
                                                                        <Setter Property="HorizontalAlignment"
                                                                                Value="Stretch" />
                                                                        <Setter Property="HorizontalContentAlignment"
                                                                                Value="Stretch" />
                                                                    </Style>
                                                                </ListView.ItemContainerStyle>
                                                                <ListView.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                                  HorizontalAlignment="Stretch"
                                                                                  HorizontalContentAlignment="Stretch">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="*" />
                                                                                </Grid.ColumnDefinitions>

                                                                                <TextBlock Text="{Binding}"
                                                                                           TextWrapping="NoWrap"
                                                                                           FontSize="12" />
                                                                            </Grid>
                                                                        </CheckBox>
                                                                    </DataTemplate>
                                                                </ListView.ItemTemplate>
                                                            </ListView>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </MenuFlyoutItem.Template>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </Grid>

                            <Rectangle x:Name="VerticalGridLine"
                                       Grid.Column="1"
                                       Width="1"
                                       Fill="#E0E0E0"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Stretch" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
